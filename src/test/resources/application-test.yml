spring:
  jpa:
    hibernate:
      ddl-auto: create-drop # 로컬에서는 create 사용
    show-sql: true      # 로컬에서는 SQL 로그 보기

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/test
    username: ssafy
    password: 1q2w3e4rQ!!

  ai:
    openai:
      api-key:
        url:
      models:
        first_request:
          model: gpt-4.1-nano
        second_request:
          model: gpt-4.1-mini
        embedding:
          model: text-embedding-3-small
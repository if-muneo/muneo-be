spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/muneo
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create  # 운영에서는 validate 사용
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # 이 부분이 빠져있어서 에러가 발생합니다!
  ai:
    openai:
      api-key: test-api-key